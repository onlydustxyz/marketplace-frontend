fragment GithubIssueId on GithubIssues {
  id
}

fragment GithubIssue on GithubIssues {
  ...GithubIssueId
  repoId
  issueNumber
  title
  htmlUrl
  authorId
  assigneeIds
  type
  status
  createdAt
  closedAt
  mergedAt
  ignoredForProjects {
    ...IgnoredGithubIssueId
  }
}
