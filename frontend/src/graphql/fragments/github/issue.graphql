fragment LiveGithubIssueId on Issue {
  id
}

fragment LiveGithubIssue on Issue {
  ...LiveGithubIssueId
  repoId
  number
  type
  status
  title
  htmlUrl
  createdAt
  closedAt
  mergedAt
  ignoredForProjects {
    ...IgnoredGithubIssueId
  }
}
