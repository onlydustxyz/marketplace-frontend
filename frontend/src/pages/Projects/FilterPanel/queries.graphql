query GetAllFilterOptions {
  projects {
    ...ProjectVisibilityDetails
    projectSponsors {
      sponsor {
        id
        name
      }
    }
    githubRepos {
      projectId
      githubRepoId
      repo {
        ...GithubRepoLanguages
      }
    }
  }
}
