query GetAllFilterOptions {
  projects {
    ...VisibleProject
    projectSponsors {
      sponsor {
        id
        name
      }
    }
    githubRepos {
      projectId
      githubRepoId
      repo {
        ...GithubRepoLanguages
      }
    }
  }
}
