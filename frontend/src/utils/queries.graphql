fragment VisibleProject on Projects {
  id
  projectLeads {
    userId
    projectId
  }
  githubRepos {
    projectId
    githubRepoId
  }
  budgets {
    id
  }
  pendingInvitations {
    id
    githubUserId
  }
}
