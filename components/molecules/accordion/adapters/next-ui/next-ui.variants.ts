import { tv } from "tailwind-variants";

export const AccordionNextUiVariants = tv({
  slots: {
    base: "flex flex-col gap-4 p-0",
    heading: "rounded-lg bg-container-4 px-3 py-2",
    trigger: "p-0",
    content: "mt-4 p-0",
  },
  variants: {},
  defaultVariants: {},
});
