import { tv } from "tailwind-variants";

export const CardProjectDefaultVariants = tv({
  slots: {
    base: "flex flex-row items-stretch justify-start gap-2 p-2",
  },
  variants: {},
  defaultVariants: {},
});
