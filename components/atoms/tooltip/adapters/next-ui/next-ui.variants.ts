import { tv } from "tailwind-variants";

export const TooltipNextUiVariants = tv({
  slots: {
    wrapper: "",
    tooltip:
      "rounded-lg border border-container-stroke-separator bg-container-action p-2 text-xs text-text-1 shadow-none",
  },
});
