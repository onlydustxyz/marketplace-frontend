import { SERVER_ACTION_PATH as path } from "./path";
import queries from "./queries";
import { SERVER_ACTIONS_TAGS as tags } from "./tags";

const ServerActionApi = {
  tags,
  queries,
  path,
};

export default ServerActionApi;
