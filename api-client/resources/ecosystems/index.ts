import adapters from "./adapters";
import * as mutations from "./mutations";
import * as queries from "./queries";
import tags from "./tags";

export const ecosystemsApiClient = {
  adapters,
  mutations,
  queries,
  tags,
};
