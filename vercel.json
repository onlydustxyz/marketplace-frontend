{
    "rewrites": [
        {
            "source": "/(.*)",
            "destination": "/"
        }
    ],
    "buildCommand": "yarn frontend:build",
    "installCommand": "yarn frontend:install",
    "outputDirectory": "frontend/dist",
    "devCommand": "yarn frontend:dev"
}
