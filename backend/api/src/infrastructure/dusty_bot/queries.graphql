fragment GithubUser on User {
  id
  login
  avatarUrl
  htmlUrl
}

fragment GithubIssue on Issue {
  id
  repoId
  number
  title
  author {
    ...GithubUser
  }
  htmlUrl
  status
  createdAt
  updatedAt
  closedAt
}

mutation CreateIssue($repoId: GithubRepoId!, $title: String!, $description: String!) {
  internalCreateIssue(repoId: $repoId, title: $title, description: $description) {
    ...GithubIssue
  }
}
