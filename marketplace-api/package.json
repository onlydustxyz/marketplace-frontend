{
  "name": "onlydust-marketplace-hasura",
  "version": "1.0.0",
  "scripts":{
    "metadata:import": "source ../.env && hasura metadata export",
    "metadata:load": "source ../.env && hasura metadata apply"
  },
  "dependencies": {
    "hasura-cli": "^2.14.0"
  }
}
