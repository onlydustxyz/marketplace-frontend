table:
  name: payment_stats
  schema: public
select_permissions:
  - role: public
    permission:
      columns:
        - github_user_id
        - money_granted
        - project_id
      filter: {}
      allow_aggregations: true
