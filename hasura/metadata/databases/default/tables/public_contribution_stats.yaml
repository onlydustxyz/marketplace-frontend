table:
  name: contribution_stats
  schema: public
select_permissions:
  - role: public
    permission:
      columns:
        - github_user_id
        - paid_count
        - total_count
        - unpaid_count
        - max_date
        - min_date
        - project_id
      filter: {}
      allow_aggregations: true
