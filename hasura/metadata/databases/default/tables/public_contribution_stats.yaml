table:
  name: contribution_stats
  schema: public
select_permissions:
  - role: public
    permission:
      columns:
        - count
        - github_user_id
        - max_date
        - min_date
        - project_id
      filter: {}
      allow_aggregations: true
