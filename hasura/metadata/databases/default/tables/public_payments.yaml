table:
  name: payments
  schema: public
select_permissions:
  - role: public
    permission:
      columns:
        - reason
        - receipt
        - amount
        - currency_code
        - id
        - recipient_id
      filter:
        recipient_id:
          _eq: X-Hasura-User-Id
