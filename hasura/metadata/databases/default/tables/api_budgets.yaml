table:
  name: budgets
  schema: api
configuration:
  column_config: {}
  custom_column_names: {}
  custom_name: Budgets
  custom_root_fields:
    select: budgets
    select_aggregate: budgetsAggregate
select_permissions:
  - role: public
    permission:
      columns:
        - initial_amount
        - remaining_amount
        - spent_amount
        - currency
        - id
      filter: {}
