table:
  name: project_details
  schema: public
select_permissions:
  - role: public
    permission:
      columns:
        - description
        - github_repo_id
        - project_id
      filter: {}
  - role: user
    permission:
      columns:
        - description
        - github_repo_id
        - project_id
        - telegram_link
      filter: {}
