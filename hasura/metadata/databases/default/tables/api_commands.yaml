table:
  name: commands
  schema: api
configuration:
  column_config: {}
  custom_column_names: {}
  custom_name: Commands
  custom_root_fields:
    select: commands
    select_aggregate: commandsAggregate
select_permissions:
  - role: public
    permission:
      columns:
        - processing_count
        - created_at
        - updated_at
        - id
        - project_id
      filter: {}
      allow_aggregations: true
