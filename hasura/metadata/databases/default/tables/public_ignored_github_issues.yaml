table:
  name: ignored_github_issues
  schema: public
select_permissions:
  - role: registered_user_specific
    permission:
      columns:
        - issue_number
        - repo_id
        - project_id
      filter: {}
