table:
  name: ignored_github_issues
  schema: public
select_permissions:
  - role: registered_user
    permission:
      columns:
        - project_id
        - repo_id
        - issue_number
      filter: {}
