table:
  name: contributions
  schema: public
select_permissions:
  - role: public
    permission:
      columns:
        - github_issue_id
        - github_user_id
        - issue_number
        - repo_id
        - created_at
        - project_id
      filter: {}
