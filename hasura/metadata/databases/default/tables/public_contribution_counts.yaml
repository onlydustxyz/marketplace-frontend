table:
  name: contribution_counts
  schema: public
select_permissions:
  - role: public
    permission:
      columns:
        - github_user_id
        - paid_count
        - unpaid_count
        - week
        - year
      filter: {}
